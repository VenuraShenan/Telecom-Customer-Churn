document.addEventListener('DOMContentLoaded', () => {
    const form = document.querySelector('form');
    form.addEventListener('submit', (event) => {
        // Optional: Add custom validation or interactivity here
        console.log('Form submitted!');
    });
});
